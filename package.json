{
  "name": "portfolio",
  "version": "1.0.0",
  "description": "Portfolio website",
  "scripts": {
    "build": "Set-Location PORT; npm install; npm run build",
    "start": "Set-Location PORT; npm run preview",
    "dev": "Set-Location PORT; npm run dev",
    "install-deps": "Set-Location PORT; npm install",
    "preview": "Set-Location PORT; npm run preview"
  },
  "engines": {
    "node": ">=18.0.0"
  },
  "workspaces": [
    "PORT"
  ]
}
